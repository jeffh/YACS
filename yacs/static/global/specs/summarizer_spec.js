// Generated by CoffeeScript 1.3.1
(function() {

  describe('summarize', function() {
    it('shortens large amounts of text', function() {
      var handler, p;
      p = document.createElement('p');
      $(p).text('this is a lot of text. No, really.');
      summarize(p, {
        summary_length: 5
      });
      expect($(p).text()).toEqual('this ... (more)');
      handler = $(p).data('summarize-click-handler');
      handler.apply($(p).find('.read-more').get(0));
      return expect($(p).text()).toEqual('this is a lot of text. No, really.');
    });
    return it('does nothing for short text', function() {
      var p;
      p = document.createElement('p');
      $(p).text('The cake is a lie');
      summarize(p);
      return expect($(p).text()).toEqual('The cake is a lie');
    });
  });

}).call(this);
