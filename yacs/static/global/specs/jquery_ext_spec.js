// Generated by CoffeeScript 1.3.1
(function() {

  describe('$.fn.checked', function() {
    var newInput;
    newInput = function(type, checked) {
      var el;
      el = document.createElement('input');
      el.type = type;
      if (checked != null) {
        el.checked = checked;
      }
      return el;
    };
    it('should return false for unchecked value', function() {
      var ignored_input, input;
      input = newInput('checkbox');
      ignored_input = newInput('checkbox', 'checked');
      return expect($([input, ignored_input]).checked()).toBeFalsy();
    });
    it('should return true for checked value', function() {
      var input;
      input = newInput('radio', 'checked');
      return expect($(input).checked()).toBeTruthy();
    });
    it('should check all checkboxes and radios if given true', function() {
      var input1, input2, p, selection;
      input1 = newInput('checkbox');
      input2 = newInput('radio');
      p = document.createElement('p');
      selection = $([input1, input2, p]);
      expect(selection.checked(true)).toEqual(selection);
      expect(input1.checked).toBeTruthy();
      expect(input2.checked).toBeTruthy();
      return expect(p.checked).toBeUndefined();
    });
    return it('should uncheck all checkboxes and radios if given false', function() {
      var input1, input2, p, selection;
      input1 = newInput('checkbox', 'checked');
      input2 = newInput('radio', 'checked');
      p = document.createElement('p');
      selection = $([input1, input2, p]);
      expect(selection.checked(false)).toEqual(selection);
      expect(input1.checked).toBeFalsy();
      expect(input2.checked).toBeFalsy();
      return expect(p.checked).toBeUndefined();
    });
  });

}).call(this);
